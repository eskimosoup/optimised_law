<%= form_for @academy_entry_download, url: download_academy_entry_path, html: { id: 'email-form' } do |f| %>
  <h6 class="white-centred">Send me this Guide</h6>

  <% if @academy_entry_download.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize @academy_entry_download.errors.count, 'error' %> prevented this message from being sent
      </h2>

      <ul>
        <% @academy_entry_download.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= render 'academy_entry_downloads/form_fields', f: f %>
<% end %>
